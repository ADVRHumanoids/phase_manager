if(BUILD_TESTING)
    add_executable(testPhaseManager letstry.cpp)
    target_link_libraries(testPhaseManager PRIVATE ${LIBRARY_TARGET_NAME})
    add_test(NAME testPhaseManager COMMAND testPhaseManager)
endif()
